<h1>Teams</h1>

<% if teams.any? %>
  <div id="teams">
    <% teams.each do |team| %>
      <div class="team">
        <h2><%= team.name %></h2>
      </div>
    <% end %>
  </div>
<% else %>
  <p class="placeholder">There are no teams yet.</p>
<% end %>